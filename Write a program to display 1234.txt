#include <stdio.h>

int main() {
    int rows = 5, c;

    for (int i = 1; i <= rows; i++) {
        c = i;
        printf("\n");
        for (;;) {
            if (c == 0)
                break;
            printf("%d ", c);
            c--;
        }
    }

    return 0;
}
